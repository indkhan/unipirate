EmptyState — dashed border, hollow station dot ("no stops recorded yet"), calm copy, one action.

```jsx
<EmptyState title="No applications yet"
  body="Once you shortlist a programme, it appears here with its deadlines tracked."
  action={<Button variant="secondary">Find programmes</Button>} />
```

Never apologetic, never illustrated, never more than one action.
