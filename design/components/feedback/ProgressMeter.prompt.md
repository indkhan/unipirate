ProgressMeter — "n of m" bar. Blue fill (progress = journey), mono count, 240ms width transition.

```jsx
<ProgressMeter label="Documents ready" value={4} max={7} />
```

Use for countable sets (documents, applications). Journey-level progress is the RouteLine, not this.
